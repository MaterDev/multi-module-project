/**
 * This file defines an object, Constants, which contains constants used throughout the project.
 * These constants can be easily accessed and modified in one place, improving maintainability.
 * Currently, the file contains a single constant, rootPackage, which stores the root package name
 * for the project.
 */

// Define an object called Constants to store constants for the project
object Constants {
  // Define a constant called rootPackage which holds a string representing the root package name
  val rootPackage = "org.typelevel"
}
